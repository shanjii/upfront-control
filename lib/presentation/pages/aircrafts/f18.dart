import 'package:flutter/material.dart';

class F18Page extends StatelessWidget {
  const F18Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Column(
          children: [],
        ),
      ),
    );
  }
}
